apiVersion: v1
kind: Service
metadata:
  name: k8s-tp-app-service
spec:
  type: NodePort
  selector:
    app: k8s-tp-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
